# Basic example

Creates an Automation Account with a system-assigned identity and configurable environment tags. The example calls the root module with no child-resource maps, so all optional child resources remain disabled.
