# Runbook example

Calls the root module once to create a runbook, daily schedule, job association, webhook, and Automation variable. Dependencies are expressed through the stable `hello` and `daily` map keys.
